spring:
  jpa:
    show-sql: false
  h2:
    console:
      enabled: false

server:
  port: ${PORT:8080}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://mehedihasanb.github.io}

security:
  jwt:
    secret: ${JWT_SECRET:change-this-secret-key-at-least-32-characters-long}
